{
  "name": "billing-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap": "cap",
    "prepare-app": "node scripts/prepare-mobile.js",
    "build": "npm run prepare-app",
    "sync": "npm run build && cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "typescript": "^6.0.3"
  }
}
